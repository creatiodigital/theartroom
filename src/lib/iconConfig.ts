/**
 * Icon configuration constants
 * Use these values for consistent icon styling across the app
 */

// Default stroke width for all lucide-react icons
// Using a thin stroke for a refined, elegant appearance
export const ICON_STROKE_WIDTH = 1.25

// Default icon sizes
export const ICON_SIZE = {
  xs: 12,
  sm: 16,
  md: 20,
  lg: 24,
  xl: 32,
} as const
